---
description: 批量为本次会话修改过的核心文件(JS/TS/React)生成单元测试
---

---
description: "批量为本次会话修改过的核心文件(JS/TS/React)生成单元测试"
---

# 🧪 Batch Unit Test Generator (批量测试生成器)

你现在的任务是扫描**所有未提交的变更（Uncommitted Changes）**，并为其中的逻辑/组件文件生成对应的测试代码。

## Step 1: Change Detection & Filtering (变更检测)
1. **Scan**: 检查 `git status` 或会话修改记录，找出所有修改过或新增的文件。
2. **Filter**: 排除以下类型的文件，**只保留**需要测试的核心代码：
   - 排除: `.css`, `.scss`, `.json`, `.md`, `config.js`, `.d.ts`。
   - **保留**: `.js`, `.ts`, `.jsx`, `.tsx` (React组件, Hooks, Utils)。
3. **Report**: 在生成代码前，先列出你打算为哪些文件写测试：
   > "检测到变动文件：[A.tsx, utils.ts]。即将生成 2 个测试文件。"

## Step 2: Environment Check (环境确认)
*快速确认测试框架（Vitest/Jest）和 React Testing Library 是否就绪（同上个版本）。*

## Step 3: Generation Loop (循环生成)
**对于每一个目标文件**，执行以下逻辑（不要合并在一起，每个文件生成独立的测试代码块）：

1. **Context Analysis**: 读取该文件的代码，理解其 Props、State 或输入输出。
2. **Strategy**: 针对该文件选择 2-3 个核心测试点 (Happy Path + Edge Case)。
3. **Mocking**: 如果该文件引用了其他文件（特别是 API 请求），请使用 `vi.mock` 自动生成 Mock。
4. **Code Output**: 生成对应的 `RunningTestName.test.tsx`。
5. 测试文件必须与源文件同级，后缀为 .test.tsx

## Step 4: Verification Command (验证命令)
生成一个可以运行**本次新生成的这几个测试**的组合命令。

---

**Output Template (输出示例)**:

> 🔍 **检测范围**
> 发现本次修改了 2 个核心文件，准备生成测试：
> 1. `src/components/Auth/LoginForm.tsx`
> 2. `src/utils/calcTax.ts`
>
> ---
>
> 📄 **1. tests/LoginForm.test.tsx**
> ```typescript
> // ... LoginForm 的测试代码 ...
> ```
>
> 📄 **2. tests/calcTax.test.ts**
> ```typescript
> // ... calcTax 的测试代码 ...
> ```
>
> 🚀 **批量验证命令:**
> `npx vitest run LoginForm calcTax`